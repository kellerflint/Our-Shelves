{
  "type": "module",
  "dependencies": {
    "pm2": "^6.0.13"
  },
  "scripts": {
    "e2e": "playwright test",
    "e2e:headed": "playwright test --headed",
    "e2e:report": "playwright show-report",
    "e2e:up": "cd backend && docker compose -f docker-compose.test.yml up -d",
    "e2e:down": "cd backend && docker compose -f docker-compose.test.yml down -v",
    "e2e:ci": "npm run e2e:up && playwright test && npm run e2e:down"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "cross-env": "^10.1.0",
    "dotenv": "^17.2.3",
    "mysql2": "^3.15.3"
  }
}
